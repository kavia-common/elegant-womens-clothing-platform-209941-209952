# Server
PORT=3001
HOST=0.0.0.0
NODE_ENV=development

# CORS: single origin or comma-separated list
# Example for local React dev:
# CORS_ORIGIN=http://localhost:3000
CORS_ORIGIN=

# JWT
JWT_SECRET=please_set_a_secure_random_value
JWT_EXPIRES_IN=7d

# Database connection (choose one of the two approaches)

# 1) Single DATABASE_URL (overrides individual PG* vars when set)
# DATABASE_URL=postgres://USER:PASSWORD@HOST:PORT/DBNAME

# 2) Individual PG* variables
PGHOST=localhost
PGPORT=5432
PGDATABASE=elegant_clothing
PGUSER=postgres
PGPASSWORD=postgres

# Optional: bcrypt cost factor (defaults to 10 if unset)
# BCRYPT_SALT_ROUNDS=10
